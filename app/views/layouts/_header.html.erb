<nav class="navbar navbar-inverse navbar-static-top">

    <div class="container-fluid">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
                <span class="sr-only"> Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
                <%= link_to 'List Contact', root_url, class: 'navbar-brand' %>
        </div>

        <!-- Conteúdo exibido em telas maiores e que serão excondidos em telas menores --->
        <div class="collapse navbar-collapse" id="expanded-menu">
            <ul class="nav navbar-nav">
                <% if !user_signed_in? %>
                    <li class="active">
                        <%= link_to 'Nova Conta', new_user_path %>
                    </li>
                <% end %>
            </ul>

                <ul class="nav navbar-nav navbar-right">

                    <% if !user_signed_in? %>
                        <li>
                            <%= link_to 'Entrar', entrar_path %>
                        </li>
                    <% else %>

                    <li class="dropdown">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
                        Bem vindo, <%= current_user.name %> <span class=""></span>
                        </a>
                            <ul class="dropdown-menu">
                                <li>
                                    <%= link_to 'Entrar', '#' %>
                                </li> 
                                    <li class="divider" role="separator"></li>
                                
                            </ul>
                            <li>
                                <%= link_to 'Minha Página', contacts_path %>
                            </li>
                            <li>
                                <%= link_to 'Editar', edit_user_path(current_user) %>
                            </li>
                            <li>
                               <%= link_to 'Sair', sair_path, method: :delete %>
                            </li>
                    </li>
                <% end %>
            </ul>
        </div>
    </div>
</nav>

